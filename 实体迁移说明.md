# 实体迁移说明

## 迁移日期
2026-03-31

## 迁移原因
- `penddingEntity` 目录用于存放临时/待处理的实体
- 已正式使用并完善实体应迁移到 `pojo` 目录
- 保持代码结构清晰，便于维护

## 已迁移实体

### 预约管理相关
| 实体名 | 类型 | 说明 |
|--------|------|------|
| `Booking.java` | Entity | 预约记录 |
| `BookingStatus.java` | Enum | 预约状态枚举 |
| `BookingSource.java` | Enum | 预约来源枚举 |

### 核销记录相关
| 实体名 | 类型 | 说明 |
|--------|------|------|
| `CheckinTicket.java` | Entity | 核销券 |
| `TicketStatus.java` | Enum | 核销券状态枚举 |

### 教练课程相关
| 实体名 | 类型 | 说明 |
|--------|------|------|
| `CoachClass.java` | Entity | 教练课程记录 |
| `CoachClassStatus.java` | Enum | 课程状态枚举 |

## 迁移操作

### 1. 更新包名
```bash
# 从 package org.example.fitnessjava.pojo.penddingEntity
# 到   package org.example.fitnessjava.pojo
```

### 2. 移动文件
```bash
# 从 src/main/java/org/example/fitnessjava/pojo/penddingEntity/
# 到   src/main/java/org/example/fitnessjava/pojo/
```

### 3. 更新导入
```bash
# 从 import org.example.fitnessjava.pojo.penddingEntity.*
# 到   import org.example.fitnessjava.pojo.*
```

## 剩余待迁移实体

以下实体仍在 `penddingEntity` 目录，待后续评估迁移：

| 实体名 | 说明 | 使用状态 |
|--------|------|---------|
| `BodyAssessmentRecord.java` | 体测记录 | 待评估 |
| `CartItem.java` | 购物车项 | 待评估 |
| `CoachScheduleDate.java` | 教练排班日期 | 待评估 |
| `CoachScheduleSlot.java` | 教练可预约时段 | 待评估 |
| `MemberProfile.java` | 学员档案 | 待评估 |
| `NotificationItem.java` | 通知项 | 待评估 |
| `NotificationType.java` | 通知类型枚举 | 待评估 |
| `ProductSnapshot.java` | 商品快照 | 待评估 |

## 迁移验证

- ✅ 编译通过：`mvn clean compile`
- ✅ 无引用错误
- ✅ 实体关系正常

## 注意事项

1. **Entity 注解**: 迁移后保持原有的 `@Entity(name = "...")` 配置不变
2. **数据库映射**: 表名和字段映射不受影响
3. **VO 转换**: Service 层的 VO 转换逻辑无需修改

---

**记录人**: AI Assistant
**最后更新**: 2026-03-31
